// Base URL of the Kiwi Party backend API (the deployed web app).
// Set EXPO_PUBLIC_API_URL when running/building — e.g. your Render URL, or your
// computer's LAN IP (http://192.168.x.x:3000) when testing on a phone in dev.
export const API_URL = process.env.EXPO_PUBLIC_API_URL || "http://localhost:3000";
